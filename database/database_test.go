package database

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestDatabase(t *testing.T) {
	// TODO: Make sure the database returns the correct shape of data and some specifics
	assert.Equal(t, 1, 1)
}
